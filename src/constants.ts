/**
 * @description Energy used for new players
 */
export const DEFAULT_ENERGY = 10
/**
 * @description HP used for new players
 */
export const DEFAULT_HP = 100

/**
 * @description Time (in milliseconds) to wait before restoring energy to each
 * player
 */
export const ENERGY_TIMER_MS = 60 * 1000 // 1 minute
